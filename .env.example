# Governance System Environment Configuration Example
# Copy this file to .env and fill in actual values

# Network Configuration
LOCALNET_RPC=http://localhost:8899
DEVNET_RPC=https://devnet.helius-rpc.com/?api-key=YOUR_HELIUS_API_KEY
# Alternative: Use public Solana RPC (may have rate limits)
# HELIUS_RPC_URL=https://api.devnet.solana.com

# Anchor Configuration
ANCHOR_WALLET=keys/authority.json

# Timeout Settings (milliseconds)
TRANSACTION_TIMEOUT=60000
CONFIRMATION_TIMEOUT=30000
STEP_DELAY=1000

# Token Configuration
COMMITTEE_TOKEN_DECIMALS=9
DEFAULT_TOKEN_AMOUNT=1000000000

# USDC Configuration (localnet environment only)
USDC_DECIMALS=6
PROPOSAL_DEPOSIT_USDC=100

# Voting Power Configuration (authority is no longer a committee member, 4 independent committee members)
MEMBER_1_VOTING_POWER=10
MEMBER_2_VOTING_POWER=15
MEMBER_3_VOTING_POWER=20
MEMBER_4_VOTING_POWER=30

# Governance Parameters
# IMPORTANT: Governance parameters are now read from on-chain configuration
# The values below are for reference only and show the business rules requirements
# To modify governance parameters, use: ts-node scripts/update-governance-config.ts

# Business Rules Parameters (Reference Only):
# - Voting Period: 14 days (1209600 seconds)
# - Participation Threshold: 40% (4000 basis points)
# - Approval Threshold: 50% (5000 basis points)
# - Veto Threshold: 30% (3000 basis points)
# - Committee Fee Rate: 10% (1000 basis points)
# - Test Mode: false (production mode)

# Deposit Return Rules:
# - Proposal approved/rejected: Return 90%, 10% as committee fee
# - Proposal vetoed: 100% deposit confiscated by committee

# Parameters still read from environment variables:
PROPOSAL_DEPOSIT=1000000000  # Proposal deposit (USDC, raw amount without decimals)

# Test environment short voting period (for waiting time only, does not affect on-chain config)
TEST_VOTING_PERIOD=60

# Network Environment
NETWORK_ENVIRONMENT=localnet
